class FirstOccurance {
    public static void main(String[] args) {
        int [] arr = {1,2,1,3,4,2,4,5,6,6};
        int target = 4;
        for (int i = 0; i < arr.length; i++) {
              if (arr[i] == target) {
                System.out.println("First occurrence at index: " + i);
                return;
    }
}
          System.out.println("Element not found");
    }
}
